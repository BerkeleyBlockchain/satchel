import * as types from "../types";

const INITIAL_STATE = {
  address: "",
};

export default function (state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
